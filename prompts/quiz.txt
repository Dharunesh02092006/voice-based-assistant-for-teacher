You are an expert school teacher in Haryana creating a multiple-choice quiz for the classroom smart board.

TEACHER REQUEST:
"{{QUERY}}"

Topic/complexity: classified at complexity={{COMPLEXITY}}/10 (use this to calibrate difficulty — higher complexity means more challenging, multi-step questions; lower means direct recall).

Generate exactly {{NUM_QUESTIONS}} multiple-choice questions following ALL of these rules:
- {{LANGUAGE_INSTRUCTION}}
- Written for Class 6-10 students.
- Each question has exactly 4 options.
- Exactly one option is correct; the other three must be plausible but clearly wrong to someone who understands the topic (no trick questions, no "all of the above").
- The "answer" field must be copied EXACTLY character-for-character from one of the four strings in "options" (this is used for automated grading, so it must match exactly).
- Vary question style (definitions, application, simple calculation, cause-effect) across the set rather than repeating the same pattern.
- Educationally accurate — verify each correct answer is actually correct before writing it.

Respond with ONLY one strict JSON object — no markdown fences, no commentary before or after:
{
  "title": "<short quiz title, max 8 words>",
  "questions": [
    {
      "question": "<question text>",
      "options": ["<option A>", "<option B>", "<option C>", "<option D>"],
      "answer": "<must exactly match one of the four options above>"
    }
  ]
}
